package com.krogerServiceB;

import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class KrogerServiceBApplicationTests {

	/*
	 * @Autowired private EmployeeServiceImpl employeeService;
	 * 
	 * @MockBean private EmployeeRepository employeeRepository;
	 * 
	 * 
	 * @Test public void getEmployeesTest() { //mocking
	 * when(employeeRepository.findAll()).thenReturn(Stream .of(new Employee(02,
	 * "Dummy2", 23567.00, "active", new Department(01, "manager", "pune",
	 * "active"))) .collect(Collectors .toList()));
	 * 
	 * //expected and actuale value assertEquals(1,
	 * employeeService.getEmployees().size());
	 * 
	 * }
	 */
}
